NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
API_URL=http://127.0.0.1:3101
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up

# API_URL is the standalone API origin, available at build time and runtime.
# MCP OAuth and database/Slack secrets belong to the API service, not the web app.
